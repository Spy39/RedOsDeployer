# 🛠 Шпаргалка: Ручные команды РЕД ОС (План «Б»)

Если утилита `RedOsDeployer` недоступна, вы можете выполнить все действия вручную через терминал (с правами `su` или `sudo`).

## 1. Работа с пакетами (DNF)

**Очистка системы от мусора (если кончилось место):**
```bash
sudo dnf clean all
sudo rm -rf /var/cache/dnf/*